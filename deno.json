{
  "name": "@silverbucket/threshold",
  "description": "A simple Deno tool which parses the output of `deno coverage` for a minimum coverage threshold",
  "version": "1.0.2",
  "license": "MIT",
  "exports": "./mod.ts",
  "tasks": {
    "coverage": "deno coverage | deno run mod.ts 77 61",
    "test": "deno test --coverage --clean mod.test.ts"
  }
}
